<%= form_for home, remote: true, authenticity_token: true do |f| %>
  <%= f.label :your_message %>:
  <%= f.text_area :content, class: "form-control", data: {textarea: "home"} %>

  <%= f.submit "send", class: "btn btn-primary", data: {send: "home"} %>
<% end %>
